//キーボードマクロのファイル
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
S_WordDeleteToEnd(0);	// 単語の右端まで削除
